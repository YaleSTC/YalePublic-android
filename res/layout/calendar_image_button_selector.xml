<?xml version="1.0" encoding="utf-8"?>
<!-- Created by Stan Swidwinski
      Layout describing the unselected grid in the calendar. Note that this grid corresponds to
      "non-current" day.
-->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="fill_parent"
    android:id="@+id/calendarGridButton"
    android:layout_height="fill_parent">

        <ImageView
            android:id="@+id/calendar_button_image"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:adjustViewBounds="true"
            android:src="@drawable/calendar_grid_button_unselected"/>

        <TextView
            android:id="@+id/calendar_button_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:textStyle="bold"
            android:layout_centerVertical="true"
            android:textColor="#000000"/>

        <ImageView
            android:id="@+id/calendar_blob"
            android:paddingRight="5dp"
            android:paddingBottom="5dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:layout_alignParentBottom="true"
            android:layout_alignParentRight="true"/>

</RelativeLayout>